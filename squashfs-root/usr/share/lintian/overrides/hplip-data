# We ship the scripts in /usr/share, but they are only used in hplip and
# hplip-gui, which have the correct deps
python-script-but-no-python-dep
# Those are scripts, but they are not meant to be invoked manually, and will
# be invoked by an interpreter as needed from the code
script-not-executable
