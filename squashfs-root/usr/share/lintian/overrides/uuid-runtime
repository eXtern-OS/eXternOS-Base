# Lintian is wrong, since on sysvinit-core systems that pulls in lsb-base
# and on systemd systems this init script is masked.
# (Same check also in util-linux.lintian-overrides)
uuid-runtime: init.d-script-needs-depends-on-lsb-base etc/init.d/uuidd (line 25)
