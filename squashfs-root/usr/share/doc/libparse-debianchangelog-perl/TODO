
* Handle internal errors (i.e. non-parser-errors) in a consistent
   matter -> partly done in 1.0
* Move output functions to seperate modules
- Parse::DebianChangelog::DpkgOut
- Parse::DebianChangelog::HtmlOut
- etc.
* Include Debian version comparison and thereby allow "fuzzy matching"
   of the range options
