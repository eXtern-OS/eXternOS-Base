# vim: set sts=4 expandtab:
IM_CONFIG_SHORT="$(gettext "Custom Configuration")"

IM_CONFIG_LONG="$(gettext "Custom configuration is created by the user or administrator using editor.")"

# Not needed
#package_auto () {
#}

# Not needed
#package_menu () {
#}
