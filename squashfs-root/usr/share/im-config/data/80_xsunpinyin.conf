# vim: set sts=4 expandtab:
IM_CONFIG_SHORT="$(gettext "activate XIM for Chinese with Sunpinyin")"

IM_CONFIG_LONG="$(gettext "X input method for Chinese with Sunpinyin
 * XIM: xsunpinyin")"

# Not needed
#package_auto () {
#}

package_menu () {
    package_status xsunpinyin
}
